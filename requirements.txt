httpx>=0.27
PyYAML>=6.0
python-dotenv>=1.0
playwright>=1.45
fastapi>=0.111
uvicorn>=0.30
# Plain uvicorn has no WebSocket protocol implementation of its own — without
# this, /manual-login/vnc's websocket route accepts the TCP connection but
# can never complete the protocol upgrade, so the panel's own noVNC viewer
# has nothing to talk to. (Equivalent to installing uvicorn[standard].)
websockets>=12.0
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.2
jdatetime>=5.0
