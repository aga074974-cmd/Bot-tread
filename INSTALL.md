# راهنمای نصب صفر تا صد — Bot-tread (ربات سفارش کارگزاری مفید)

این فایل فرض می‌کند هیچ‌چیز از قبل نصب نیست و تو هم برنامه‌نویس نیستی.
هر دستور را **جدا جدا** کپی کن و در ترمینال سرور بزن، بعد سراغ بعدی برو.
اگر جایی خطا دیدی، قبل از رفتن به مرحله‌ی بعد بخش [عیب‌یابی](#عیبیابی) را ببین.

> اگر ربات از قبل روی سرورت نصب است و فقط می‌خواهی آپدیت بگیری، مستقیم برو به
> بخش [به‌روزرسانی](#بهروزرسانی-روزمره).

---

## فهرست

- [۰. قبل از شروع چه چیزهایی لازم داری](#۰-قبل-از-شروع-چه-چیزهایی-لازم-داری)
- [۱. وصل شدن به سرور](#۱-وصل-شدن-به-سرور)
- [۲. تنظیم ساعت سرور](#۲-تنظیم-ساعت-سرور)
- [۳. نصب بسته‌های سیستمی](#۳-نصب-بستههای-سیستمی)
- [۴. گرفتن کد پروژه](#۴-گرفتن-کد-پروژه)
- [۵. محیط پایتون و کتابخانه‌ها](#۵-محیط-پایتون-و-کتابخانهها)
- [۶. نصب مرورگر Playwright](#۶-نصب-مرورگر-playwright)
- [۷. ساختن فایل تنظیمات `.env`](#۷-ساختن-فایل-تنظیمات-env)
- [۸. ساختن کاربر سرویس و دادن دسترسی‌ها](#۸-ساختن-کاربر-سرویس-و-دادن-دسترسیها)
- [۹. نصب سرویس systemd](#۹-نصب-سرویس-systemd)
- [۱۰. باز کردن پورت در فایروال](#۱۰-باز-کردن-پورت-در-فایروال)
- [۱۱. اولین ورود به پنل](#۱۱-اولین-ورود-به-پنل)
- [۱۲. اولین ورود به حساب مفید (ورود دستی)](#۱۲-اولین-ورود-به-حساب-مفید-ورود-دستی)
- [۱۳. تست بی‌خطر، بعد رفتن به حالت واقعی](#۱۳-تست-بیخطر-بعد-رفتن-به-حالت-واقعی)
- [به‌روزرسانی روزمره](#بهروزرسانی-روزمره)
- [کار روزمره با پنل](#کار-روزمره-با-پنل)
- [پیام‌های خطا یعنی چه](#پیامهای-خطا-یعنی-چه)
- [پشتیبان‌گیری](#پشتیبانگیری)
- [عیب‌یابی](#عیبیابی)
- [نکات امنیتی](#نکات-امنیتی)
- [نکات ریسک — این‌ها را حتماً بخوان](#نکات-ریسک--اینها-را-حتماً-بخوان)
- [ساختار پروژه](#ساختار-پروژه)

---

## ۰. قبل از شروع چه چیزهایی لازم داری

| چیز | توضیح |
|---|---|
| یک سرور مجازی (VPS) | اوبونتو ۲۲.۰۴ یا ۲۴.۰۴، حداقل **۲ گیگ رم** و ۲۰ گیگ فضا. با ۱ گیگ رم مرورگر وسط کار کشته می‌شود. |
| دسترسی root به سرور | یعنی یوزر `root` و رمزش، یا یک یوزر با `sudo`. |
| حساب کارگزاری مفید | نام کاربری و رمز ایزی‌تریدر. |
| یک برنامه‌ی SSH روی گوشی | مثلاً Termius یا JuiceSSH. |

**چرا سرور ایرانی؟** سایت مفید از بیرون ایران معمولاً باز نمی‌شود. سرور را ایران بگیر،
وگرنه ربات به در بسته می‌خورد و مدام خطای «سایت در دسترس نیست» می‌دهد.

---

## ۱. وصل شدن به سرور

از برنامه‌ی SSH گوشی وصل شو (به‌جای `IP-سرور` آی‌پی خودت را بگذار):

```
ssh root@IP-سرور
```

از اینجا به بعد، همه‌ی دستورها داخل همین ترمینال زده می‌شوند.

---

## ۲. تنظیم ساعت سرور

**این مرحله را رد نکن.** زمان‌بندی سفارش‌ها روی ساعت سیستم است؛ ساعتِ عقب یعنی
سفارش دیر می‌رود.

```
timedatectl set-timezone Asia/Tehran
```

```
timedatectl set-ntp true
```

```
timedatectl
```

خروجی باید ساعت درست تهران و `System clock synchronized: yes` را نشان بدهد.

---

## ۳. نصب بسته‌های سیستمی

```
apt update
```

```
apt install -y python3 python3-venv python3-pip git tzdata
```

بسته‌های زیر فقط برای قابلیت **«ورود دستی»** پنل لازم‌اند (آن صفحه‌ای که مرورگرِ
داخل سرور را روی گوشی‌ات نشان می‌دهد تا خودت رمز و پیامک را وارد کنی):

```
apt install -y xvfb x11vnc novnc
```

> بدون این سه بسته، بقیه‌ی پنل کار می‌کند ولی صفحه‌ی «ورود دستی» با خطا بالا
> نمی‌آید. اگر روزی رمزت عوض شود یا کارگزاری کد پیامکی بخواهد، بدون این صفحه
> گیر می‌کنی — پس همین حالا نصبشان کن.

---

## ۴. گرفتن کد پروژه

```
mkdir -p /opt/mofid-bot
```

```
git clone -b claude/relaxed-cori-6p65ae https://github.com/aga074974-cmd/Bot-tread.git /opt/mofid-bot
```

```
cd /opt/mofid-bot
```

> `claude/relaxed-cori-6p65ae` اسم شاخه‌ای است که تغییرات روی آن انجام می‌شود.
> هر بار که چیزی اضافه می‌شود، همین شاخه آپدیت می‌گیرد.

---

## ۵. محیط پایتون و کتابخانه‌ها

```
python3 -m venv /opt/mofid-bot/.venv
```

```
/opt/mofid-bot/.venv/bin/pip install --upgrade pip
```

```
/opt/mofid-bot/.venv/bin/pip install -r /opt/mofid-bot/requirements.txt
```

اگر `pip` وسط دانلود قطع شد یا خیلی کند بود، یک بار دیگر همان دستور را بزن —
ادامه‌ی کار را از همان‌جا می‌گیرد. اگر باز هم نشد، یک میرور PyPI ایرانی روی سرور
تنظیم کن و دوباره امتحان کن.

---

## ۶. نصب مرورگر Playwright

ربات یک مرورگر واقعی (Chromium) را کنترل می‌کند، پس باید نصب شود. مرورگر را در
مسیر مشترک `/opt/pw-browsers` می‌گذاریم تا کاربر سرویس هم پیدایش کند:

```
export PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers
```

```
/opt/mofid-bot/.venv/bin/playwright install --with-deps chromium
```

اگر دانلود با خطای **۴۰۳** خورد، اول این را بزن و بعد دستور بالا را تکرار کن:

```
export PLAYWRIGHT_DOWNLOAD_HOST=https://cdn.npmmirror.com/binaries/playwright
```

---

## ۷. ساختن فایل تنظیمات `.env`

```
cp /opt/mofid-bot/.env.example /opt/mofid-bot/.env
```

```
nano /opt/mofid-bot/.env
```

داخل ویرایشگر این‌ها را پر کن (ذخیره: `Ctrl+O` بعد `Enter`، خروج: `Ctrl+X`):

| کلید | چه بگذاری |
|---|---|
| `MOFID_USERNAME` | نام کاربری ایزی‌تریدر |
| `MOFID_PASSWORD` | رمز ایزی‌تریدر |
| `PANEL_PASSWORD` | **رمزی که خودت می‌سازی** برای ورود به پنل. طولانی بگذار. |
| `SESSION_SECRET` | یک رشته‌ی تصادفی بلند. اگر خالی بماند، با هر ری‌استارت از پنل بیرون می‌افتی. |
| `DRY_RUN` | فعلاً `true` بماند (حالت تمرین، سفارش واقعی نمی‌رود). |
| `HEADLESS` | `true` بماند. |

برای ساختن `SESSION_SECRET` می‌توانی این را بزنی و خروجی‌اش را کپی کنی:

```
openssl rand -hex 32
```

**بقیه‌ی کلیدها را دست نزن** مگر بدانی چه می‌کنی. معنی‌شان:

- `GRACE_PERIOD_SECONDS=120` — اگر سرور ری‌استارت شد و سفارش تا این چند ثانیه
  عقب افتاد، باز هم اجرا می‌شود؛ بیشتر از آن، «اجرا نشد» می‌خورد.
- `RETRY_DEADLINE_SECONDS=90` — بعد از این چند ثانیه از زمان سفارش، دیگر تلاش
  تازه شروع نمی‌شود. سفارشی که ۸ دقیقه دیر برود، دیگر همان سفارش نیست.
- `MAX_RETRIES=5` — حداکثر تعداد تلاش.
- `SCREENSHOT_RETENTION_DAYS=3` — عکس‌های هر سفارش بعد از ۳ روز پاک می‌شوند.
- `ORDER_HISTORY_RETENTION_DAYS=30` — سفارش‌های قدیمی‌تر از ۳۰ روز از تاریخچه
  پاک می‌شوند.
- `STORAGE_STATE_PATH=auth_state.json` — جایی که نشستِ ورود ذخیره می‌شود.

**اگر `novnc` را جای دیگری نصب کردی** (مسیر پیش‌فرض `/usr/share/novnc` است)، این
خط را هم به `.env` اضافه کن:

```
NOVNC_DIR=/مسیر/واقعی/novnc
```

⚠️ **مواظب املا باش:** فقط `true` و `false` شناخته می‌شوند. اگر `fasle` بنویسی،
ربات آن را `true` می‌خواند (یعنی حالت تمرین) و یک هشدار در لاگ می‌نویسد. یعنی
غلط املایی باعث سفارش واقعیِ ناخواسته نمی‌شود، ولی ممکن است فکر کنی زنده‌ای و
نباشی — بعد از تغییر این خط، لاگ را چک کن.

---

## ۸. ساختن کاربر سرویس و دادن دسترسی‌ها

سرویس با یک کاربر بی‌دسترسی اجرا می‌شود، نه با root:

```
useradd -r -s /usr/sbin/nologin mofidbot
```

```
mkdir -p /opt/mofid-bot/logs
```

```
chown -R mofidbot:mofidbot /opt/mofid-bot /opt/pw-browsers
```

```
chmod 600 /opt/mofid-bot/.env
```

> `.env` رمز حساب کارگزاری‌ات را دارد؛ `chmod 600` یعنی فقط صاحبش بخواندش.
> مالکش هم باید `mofidbot` باشد، چون وقتی از پنل تیک «ذخیره برای دفعات بعد» را
> می‌زنی، خود پنل این فایل را بازنویسی می‌کند.

---

## ۹. نصب سرویس systemd

با این کار پنل با بستن SSH قطع نمی‌شود و بعد از ری‌استارت سرور هم خودش بالا می‌آید.

```
cp /opt/mofid-bot/systemd/mofid-panel.service /etc/systemd/system/
```

```
systemctl daemon-reload
```

```
systemctl enable --now mofid-panel
```

```
systemctl status mofid-panel
```

باید سبز و `active (running)` باشد. برای دیدن لاگ زنده:

```
journalctl -u mofid-panel -f
```

(با `Ctrl+C` از لاگ بیرون بیا.)

> فایل `mofid-bot.service` هم در پوشه هست، ولی آن مالِ حالت قدیمیِ `orders.yaml`
> است. **اگر از پنل استفاده می‌کنی آن را نصب نکن** — هر دو با هم یعنی هر سفارش
> دو بار.

---

## ۱۰. باز کردن پورت در فایروال

اگر فایروال `ufw` روشن است:

```
ufw allow 8080/tcp
```

```
ufw status
```

اگر سرورت پنل فایروال جدا دارد (مثل پنل بعضی هاست‌های ایرانی)، از همان‌جا هم
پورت `8080` را باز کن.

---

## ۱۱. اولین ورود به پنل

از مرورگر گوشی برو به:

```
http://IP-سرور:8080
```

رمزی که در `PANEL_PASSWORD` گذاشتی را وارد کن. باید صفحه‌ی «پنل ربات» با
ربات متحرکِ بالای صفحه بیاید.

---

## ۱۲. اولین ورود به حساب مفید (ورود دستی)

وسط هدر پنل یک **چراغ گرد** هست:

- **قرمز** = نشستِ ورود معتبر نیست (اولین بار همین‌طور است).
- **سبز** = ربات وارد حساب هست و می‌تواند سفارش بفرستد.

یک بار روی چراغ بزن؛ خودش تلاش می‌کند با نام‌کاربری/رمزِ `.env` وارد شود.
اگر نشد (کد پیامکی، کپچا، رمز عوض‌شده)، زیر چراغ لینک **«ورود دستی»** هست:

1. روی «ورود دستی» بزن.
2. مرورگرِ داخل سرور را روی گوشی‌ات می‌بینی — همان صفحه‌ی ورود مفید.
3. خودت نام کاربری، رمز و کد پیامکی را وارد کن.
4. به‌محض اینکه وارد حساب شدی، صفحه خودش می‌بندد و چراغ سبز می‌شود.
5. وقتی از صفحه بیرون بروی، مرورگر سرور هم خودکار بسته می‌شود.

**نکته‌ی مهم:** ربات هیچ‌وقت کپچا حل نمی‌کند و هیچ‌وقت «اقرارنامه و بیانیه ریسک»
را امضا نمی‌کند. این دو کارِ خودِ صاحب حساب است — از همین صفحه‌ی ورود دستی
انجامشان بده.

بعد از ورود موفق، نشست در `auth_state.json` ذخیره می‌شود و دفعات بعد لازم نیست
دوباره وارد شوی. اگر رمزت را عوض کردی، این فایل را پاک کن تا از نو وارد شود:

```
rm -f /opt/mofid-bot/auth_state.json
```

---

## ۱۳. تست بی‌خطر، بعد رفتن به حالت واقعی

تا وقتی `DRY_RUN=true` است، ربات **واقعاً وارد می‌شود، نماد را باز می‌کند و فرم
را پر می‌کند، ولی دکمه‌ی نهایی ارسال را نمی‌زند.** یعنی تست بی‌خطر.

1. **در ساعت معاملات بازار** (نه شب) یک سفارش تمرینی از پنل ثبت کن برای چند
   دقیقه‌ی بعد.
2. وقتی وضعیتش عوض شد، کنار وضعیت یک آیکون ▶ می‌آید — بزن تا گالری عکس‌ها باز
   شود.
3. عکس `..._quantity_filled` را نگاه کن: باید تعداد را درست پر کرده باشد.
4. اگر جایی گیر کرد، آخرین عکس نشان می‌دهد کجا و فایل `page.html` نشان می‌دهد چرا.
   هر دو از همان گالری قابل دانلودند — بفرستشان تا اصلاح شود.

⚠️ **حتماً در ساعت بازار تست کن.** شب، خودِ اپ می‌نویسد «خارج از ساعت معاملات!»
و ممکن است کیبورد عددی اصلاً باز نشود؛ آن‌وقت خطای طبیعی را با خرابیِ ربات
اشتباه می‌گیری.

فقط وقتی عکس‌ها درست بودند، برو به حالت واقعی:

```
nano /opt/mofid-bot/.env
```

`DRY_RUN=true` را به `DRY_RUN=false` عوض کن، ذخیره کن، بعد:

```
systemctl restart mofid-panel
```

---

## به‌روزرسانی روزمره

هر وقت تغییر جدیدی روی شاخه آمد، همین سه خط:

```
cd /opt/mofid-bot
```

```
git pull origin claude/relaxed-cori-6p65ae
```

```
systemctl restart mofid-panel
```

> اگر `git pull` گفت مالکیت پوشه مشکل دارد (`dubious ownership`)، یک‌بار این را
> بزن و دوباره امتحان کن: `git config --global --add safe.directory /opt/mofid-bot`

اگر آپدیت کتابخانه‌ی جدیدی داشت (به‌ندرت پیش می‌آید)، بعد از `git pull` این را هم بزن:

```
/opt/mofid-bot/.venv/bin/pip install -r /opt/mofid-bot/requirements.txt
```

---

## کار روزمره با پنل

- **ثبت سفارش:** نماد، خرید/فروش، تعداد، نوع سفارش، تاریخ و ساعت را بده و ثبت کن.
- **زمان باید دست‌کم یک دقیقه جلوتر از الان باشد.** اگر زمانی که زده‌ای گذشته
  باشد، پیام «وقت سفارش گذشته — زمان دیگری انتخاب کنید» می‌آید و **چیزی که
  نوشته بودی پاک نمی‌شود** — فقط ساعت را عوض کن. (این قید فقط برای امروز است؛
  برای تاریخ‌های بعدی محدودیتی نیست.)
- **تعداد خالی:** کنار عنوان «تعداد» یک `!` قرمز و نوشته‌ی «تعداد را وارد کنید»
  می‌آید و با شروع تایپ محو می‌شود.
- **سفارش‌های در انتظار** همیشه بالای صفحه‌ی اصلی می‌مانند تا بتوانی لغوشان کنی.
- **لغو:** سفارشِ لغوشده کلاً حذف می‌شود و در تاریخچه نمی‌ماند.
- **تاریخچه:** فقط سفارش‌های تمام‌شده. سه‌تای آخر در صفحه‌ی اصلی، بقیه در `/history`.
- **خطا:** روی وضعیت «ناموفق» بزن تا پیام فارسی سه ثانیه ظاهر شود.
- **عکس‌ها و فایل‌ها:** آیکون ▶ کنار وضعیت → گالری. عکس‌ها و `page.html`
  قابل دیدن و دانلودند.
- **خروج از پنل:** آدرس `/logout`.

---

## پیام‌های خطا یعنی چه

| پیام | یعنی چه | چه کار کنی |
|---|---|---|
| سایت کارگزاری مفید در دسترس نیست | سرورِ خودِ مفید جواب نمی‌دهد | کاری از دست ربات برنمی‌آید؛ بعداً امتحان کن |
| نمادی با این نام پیدا نشد | جستجو خالی برگشت | املای نماد را چک کن؛ هیچ سفارشی نرفته |
| نیاز به «اقرارنامه و بیانیه ریسک» | این نماد تا امضای خودت معامله نمی‌شود | از «ورود دستی» یک بار خودت تأییدش کن |
| سفارش رد شد — سایت می‌گوید… | خود کارگزاری رد کرده (مثلاً محدوده‌ی زمانی) | متن داخل پیام را بخوان؛ دوباره تلاش نمی‌شود |
| ورود ناموفق | نام‌کاربری/رمز یا کپچا | از «ورود دستی» وارد شو |
| اجرا نشد | سفارش خیلی دیر شد (ری‌استارت سرور و…) | دوباره ثبتش کن |

سه مورد اول و «سفارش رد شد» **تلاش دوباره نمی‌شوند** — عمدی است: نمادی که وجود
ندارد بار دوم به وجود نمی‌آید، و سفارشی که رد شده نباید دوباره فرستاده شود که
یک‌وقت دوتا ثبت نشود.

---

## پشتیبان‌گیری

سه چیز در گیت نیستند و اگر سرور را عوض کنی از دست می‌روند:

| فایل | چیست |
|---|---|
| `.env` | رمزها و تنظیمات |
| `panel.db` | همه‌ی سفارش‌ها و تاریخچه |
| `auth_state.json` | نشستِ ورود (اگر نبود، دوباره وارد می‌شوی — بحرانی نیست) |

یک کپی از آن‌ها بگیر:

```
tar czf ~/mofid-backup.tar.gz -C /opt/mofid-bot .env panel.db auth_state.json
```

---

## عیب‌یابی

**پنل بالا نمی‌آید / صفحه باز نمی‌شود**

```
systemctl status mofid-panel
```

```
journalctl -u mofid-panel -n 50 --no-pager
```

آخرین خط‌های لاگ معمولاً دقیقاً می‌گویند مشکل چیست.

| علامت | علت محتمل | درمان |
|---|---|---|
| `PANEL_PASSWORD` خالی است | رمز پنل را در `.env` نگذاشته‌ای | پرش کن و `systemctl restart mofid-panel` |
| صفحه اصلاً باز نمی‌شود | پورت بسته است | `ufw allow 8080/tcp` و پنل فایروال هاست |
| «ورود دستی» خطا می‌دهد | `xvfb` / `x11vnc` / `novnc` نصب نیست | `apt install -y xvfb x11vnc novnc` |
| صفحه‌ی ورود دستی سیاه است | مسیر noVNC فرق دارد | `NOVNC_DIR` را در `.env` تنظیم کن |
| مرورگر وسط کار می‌میرد | رم کم است | سرور ۲ گیگ یا بیشتر / swap اضافه کن |
| سفارش‌ها دیر اجرا می‌شوند | ساعت سرور غلط است | مرحله‌ی [۲](#۲-تنظیم-ساعت-سرور) |
| همه‌ی سفارش‌ها «ناموفق» با خطای ورود | نشست باطل شده | چراغ را بزن، بعد «ورود دستی» |
| `git pull` خطای `dubious ownership` | مالکیت پوشه | `git config --global --add safe.directory /opt/mofid-bot` |
| نصب مرورگر خطای ۴۰۳ | دانلود مسدود است | `PLAYWRIGHT_DOWNLOAD_HOST` مرحله‌ی [۶](#۶-نصب-مرورگر-playwright) |

**راه‌اندازی مجدد کامل (بدون از دست دادن اطلاعات):**

```
systemctl restart mofid-panel
```

---

## نکات امنیتی

- پنل روی **HTTP** است، نه HTTPS. یعنی رمز پنل رمزنگاری‌نشده رد می‌شود.
  برای استفاده‌ی شخصی روی یک آی‌پی که کسی نمی‌داند قابل قبول است، ولی:
  - `PANEL_PASSWORD` را **طولانی و بی‌ربط** بگذار (نه اسم و تاریخ تولد).
  - اگر می‌توانی، در فایروال پورت `8080` را فقط به آی‌پی خودت باز کن.
- `.env` رمز حساب کارگزاری‌ات را دارد. هیچ‌وقت commit نکن (در `.gitignore` هست)
  و `chmod 600` را نگه دار.
- هر کسی که رمز پنل را داشته باشد می‌تواند **از حساب تو سفارش بفرستد**.
- سرویس با کاربر `mofidbot` اجرا می‌شود نه root — این عمدی است، تغییرش نده.

---

## نکات ریسک — این‌ها را حتماً بخوان

- **API رسمی نیست.** ربات دقیقاً کاری را می‌کند که تو با انگشت روی سایت
  می‌کنی، فقط خودکار. استفاده‌ی خودکار از اپ ریتیل ممکن است با قوانین کارگزاری
  مفید در تناقض باشد و در بدترین حالت به مسدود شدن حساب برسد. مسئولیتش با توست.
- **ربات کپچا حل نمی‌کند** و **اقرارنامه‌ی ریسک را امضا نمی‌کند.** این‌ها عمدی
  است، نه نقص.
- **قبل از هر عددی، نماد چک می‌شود.** اگر نام روی تیکت با نمادی که خواسته‌ای
  یکی نبود، سفارش متوقف می‌شود — سفارشِ درست روی نمادِ اشتباه بدترین اتفاق ممکن است.
- **تعداد بعد از پر شدن دوباره خوانده می‌شود** و اگر با چیزی که خواستی یکی
  نبود، سفارش ارسال نمی‌شود.
- **پیام موفقیت هنوز روی یک سفارش واقعی تأیید نشده.** اگر ربات گفت پیام موفقیت
  را نشناخت، **الزاماً یعنی سفارش ثبت نشده نیست** — حتماً خودت در اپ مفید چک کن.
- پنل و حالت `orders.yaml` دو سیستم جدا هستند؛ هم‌زمان اجرایشان نکن.

---

## ساختار پروژه

```
/opt/mofid-bot
├── main.py                     نقطه‌ی ورود حالت قدیمی (orders.yaml)
├── .env                        تنظیمات و رمزها  (در گیت نیست)
├── panel.db                    سفارش‌ها          (در گیت نیست)
├── auth_state.json             نشست ورود        (در گیت نیست)
├── bot/
│   ├── config.py               خواندن .env
│   ├── models.py               مدل سفارش
│   ├── scheduler.py            انتظار تا زمان سفارش + تلاش دوباره
│   ├── screenshots.py          ذخیره و پاکسازی عکس‌ها
│   └── broker/
│       ├── base.py             اینترفیس کارگزاری + انواع خطا
│       └── mofid_playwright.py کنترل واقعی مرورگر روی سایت مفید
├── panel/
│   ├── main.py                 سرور پنل و آدرس‌ها
│   ├── db.py                   ذخیره‌سازی SQLite
│   ├── errors.py               ترجمه‌ی خطاها به فارسی
│   ├── manual_login.py         ورود دستی (Xvfb + x11vnc + noVNC)
│   └── templates/              صفحه‌های HTML پنل
├── systemd/                    فایل‌های سرویس
├── tests/                      تست‌های خودکار روی سایت ساختگی
└── logs/screenshots/           عکس و page.html هر سفارش
```

---

**پشتیبانی:** اگر جایی گیر کردی، این دو تا را بفرست — تقریباً همیشه کافی‌اند:

```
journalctl -u mofid-panel -n 50 --no-pager
```

و عکس‌های همان سفارش از گالری پنل (آیکون ▶ کنار وضعیت).
